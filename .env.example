# Copy this file to .env and fill in your values.

# The WhatsApp group's JID (e.g. 120363012345678901@g.us).
# Get this by running `npm run start` once, sending any message in the
# group, and reading it from the console log ("Discovered group JID: ...").
GROUP_JID=

# Optional: a personal number (in JID form, e.g. 233241234567@s.whatsapp.net)
# that idle-member reports get DMed to instead of posted in the group.
# Leave blank to post idle reports directly in the group.
REPORT_TO_JID=

# Phone number (with country code, digits only, e.g. 233241234567) for the
# bot's dedicated WhatsApp account. Only needed for pairing-code login.
BOT_PHONE_NUMBER=

# IANA timezone used for all schedules.
TIMEZONE=Africa/Accra

# --- Dashboard ---

# Port the dashboard's Express server listens on *inside* the container.
# Leave this at 3000 — it's an internal detail, not what you connect to.
PORT=3000

# The *host-side* port the dashboard is published to on 127.0.0.1 (see
# docker-compose.yml) — reached through your own nginx/reverse proxy,
# which handles the public hostname and TLS. Only change this if
# something else on the VPS already has 3000 in use; if you do, update
# your nginx server block's proxy_pass to match.
HOST_PORT=3000

# HTTP Basic Auth credentials for the dashboard. Pick a strong password —
# anyone with these can post to the group, rename it, and change its icon.
ADMIN_USER=admin
ADMIN_PASSWORD=
